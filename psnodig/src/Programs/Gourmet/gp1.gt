func BirkSort(A) {
    for i := 0, length(A) {
        j := i
        while (j > 0) && (A[j-1] > A[j]) {
            tmp := A[i]
            A[i] := A[j]
            A[j] := tmp
            j := j - 1
        }
    }
}

BirkSort([1, 3, 2])