?Two integers $length$ and $width$?
!A boolean revealing whether or not the input's area is greater than that of the Italian Republic!

func AreaGreaterThanItaly(length int, width int) {
    area := length * width
    if area > 301340 {
        return true
    }
    else {
        return false
    }
}