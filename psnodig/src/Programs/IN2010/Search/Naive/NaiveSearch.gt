func Search(A int*, x int) {
    # n := length(A)
    i := 0
    while i != n {
        if A[i] == x {
            return true
        }
        i := i + 1
    }
    return false
}

Search([1, 2, 3], 5)